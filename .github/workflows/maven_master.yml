name: Python CI for Development branch

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:

  build:
    uses: anchoranalysis/anchor-python-cicd/.github/workflows/maven_master.yml@main
    with:
      library-name: anchor-python-visualization
    secrets: inherit
