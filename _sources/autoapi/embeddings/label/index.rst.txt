:py:mod:`embeddings.label`
==========================

.. py:module:: embeddings.label

.. autoapi-nested-parse::

   Routines for loading embeddings from CSV and adding identifiers and labels.



Module Contents
---------------

Classes
~~~~~~~

.. autoapisummary::

   embeddings.label.LabelledFeatures




.. py:class:: LabelledFeatures(features: pandas.DataFrame, labels: pandas.Series, image_paths: Optional[pandas.Series] = None)

   Maintains separate data-frames for embeddings and labels, but with the same number and order of rows

   .. py:method:: number_items(self) -> int

      Returns the number of items (i.e. rows) in the data-frames/series.


   .. py:method:: sample_without_replacement(self, sample_size: int) -> LabelledFeatures

      Samples without replacement (taking identical rows from each member data-frame/series).

      :param sample_size: number of items to sample
      :raises InsufficientRowsException: if there are fewer rows available than :code:`sample_size`



